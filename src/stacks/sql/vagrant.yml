core:
  includes:
    - components/sql/core

management:
  includes:
    - stacks/sql/core
  chef:
    json:
      gusztavvargadr_workstations_core:
        tools:
          chocolatey:
            jetbrainstoolbox:
            datagrip:

engine:
  includes:
    - stacks/sql/core
    - components/sql/v17d
