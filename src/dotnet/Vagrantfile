local_directory = File.expand_path('../', __FILE__)
core_directory = "#{local_directory}/../core"

require "#{core_directory}/Vagrantfile.common"

Vagrant.configure('2') do |config|
  vagrant_config_vm_define config, local_directory, 'w16s-sql14d-vs15c', 'gusztavvargadr/w16s-sql14d-vs15c', true, 8192, 4
end
