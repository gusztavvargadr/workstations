local_directory = File.expand_path('../', __FILE__)
core_directory = "#{local_directory}/../core"

require "#{core_directory}/Vagrantfile.core"

Vagrant.configure('2') do |config|
  vagrant_config_vm_define config, local_directory, 'w16s-vs10p' do |w16s_vs10p|
  end
end

Vagrant.configure('2') do |config|
  vagrant_config_vm_define config, local_directory, 'w16s-vs15c' do |w16s_vs15c|
  end
end

Vagrant.configure('2') do |config|
  vagrant_config_vm_define config, local_directory, 'w16s-vs15p' do |w16s_vs15p|
  end
end

Vagrant.configure('2') do |config|
  vagrant_config_vm_define config, local_directory, 'w16s-vs17c' do |w16s_vs17c|
  end
end

Vagrant.configure('2') do |config|
  vagrant_config_vm_define config, local_directory, 'w16s-vs17p' do |w16s_vs17p|
  end
end
