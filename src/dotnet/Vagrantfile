local_directory = File.expand_path('../', __FILE__)
core_directory = "#{local_directory}/../core"

require "#{core_directory}/Vagrantfile.core"

Vagrant.configure('2') do |config|
  vagrant_config_vm_define config, local_directory, 'w16s-sql14d-vs15c',
    memory: 8192,
    cpus: 4,
    chef_roles: ['gusztavvargadr_workstations_sql'] do |w16s_sql14d_vs15c|
  end
end
